version: "3.9"

services:
  agent:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    env_file:
      - ../../.env
    volumes:
      - ../../data:/app/data
    restart: unless-stopped
